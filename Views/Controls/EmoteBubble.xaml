<UserControl x:Class="DesktopPet.Views.Controls.EmoteBubble"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <Storyboard x:Key="FloatAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="BubbleGrid"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="0" To="-5" Duration="0:0:0.8" />
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="BubbleGrid">
        <Grid.RenderTransform>
            <TranslateTransform Y="0" />
        </Grid.RenderTransform>

        <StackPanel HorizontalAlignment="Center">
            <!-- Mũi nhọn hướng lên (khi bubble nằm DƯỚI pet) -->
            <Polygon x:Name="TopPointer" Points="6,0 0,8 12,8" Fill="#FFFFFFFF" Stroke="#FF80AB" StrokeThickness="1"
                     HorizontalAlignment="Center" Margin="0,0,0,-1" Visibility="Collapsed" />

            <!-- Khung bong bóng chat -->
            <Border Background="#FFFFFFFF" BorderBrush="#FF80AB" BorderThickness="1.8"
                    CornerRadius="14" Padding="12,6" MaxWidth="260"
                    Effect="{DynamicResource ShadowEffect}">
                <TextBlock x:Name="EmoteTextBlock" Text="❤️" FontSize="12" FontWeight="SemiBold"
                           Foreground="#D81B60" HorizontalAlignment="Center" TextAlignment="Center" 
                           TextWrapping="Wrap" LineHeight="16" SnapsToDevicePixels="True" />
            </Border>

            <!-- Mũi nhọn hướng xuống thú cưng (khi bubble nằm TRÊN pet) -->
            <Polygon x:Name="BottomPointer" Points="0,0 12,0 6,8" Fill="#FFFFFFFF" Stroke="#FF80AB" StrokeThickness="1"
                     HorizontalAlignment="Center" Margin="0,-1,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
